linters:
  enable:
  - ginkgolinter
  - revive
  - gocognit
  - unparam
linters-settings:
  ginkgolinter:
    forbid-focus-container: true
  revive:
    rules:
    - name: dot-imports
      arguments:
      - allowedPackages:
        - github.com/onsi/ginkgo/v2
        - github.com/onsi/gomega
