name: PR Quality Check

on:
  pull_request:
    branches: ["ise"]
  workflow_dispatch:
  workflow_call:

jobs:
  Quality_Check:
    name: Quality Check
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3

      # - name: Linting
      #   run: |
      #     make format
      #     make lint

      # There is 1 failing test that need to be fixed, so commenting out for now.
      - name: Run Unit Tests
        run: |
          make test
