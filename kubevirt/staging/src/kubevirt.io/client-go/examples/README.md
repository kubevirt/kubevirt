## Kubevirt client-go usage examples.

- [listvms](listvms)
    List all VMs and VMIs objects in KubeVirt
    ```
    $ ./listvms
    Type                       Name          Namespace     Status
    VirtualMachine             vm-cirros     default       true
    VirtualMachineInstance     vm-cirros     default       Running

    ```