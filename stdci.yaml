---
sub-stages:
  - k8s-1.13.0-sriov:
      runtime_requirements:
        sriovnic: true
        # todo: do we need to duplicate the following parameters?..
        support_nesting_level: 2
        isolation_level: container
runtime_requirements:
  support_nesting_level: 2
  isolation_level: container
reporting:
  style: stdci
