# golangci configuration
run:
  timeout: 10m
  skip-dirs:
    - vendor

linters-settings:
  forbidigo:
    forbid:
      - Log.*\.V(.*).*Error[f]?(.*)
      - Log.*\.V(.*).*Warning[f]?(.*)
      - Log.*\.V\([0-9]\).*Info[f]?(.*)

linters:
  disable-all: true
  enable:
    - forbidigo
