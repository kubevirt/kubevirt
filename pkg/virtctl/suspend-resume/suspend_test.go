package suspend

import (
	. "github.com/onsi/ginkgo"
	. "github.com/onsi/gomega"
)

var _ = Describe("Suspend And Resume", func() {

	It("has no test yet", func() {
		Expect(0).To(BeZero(), "let's keep imports at least already :)")
	})

})
