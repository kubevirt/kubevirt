apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: dra-test-driver
  namespace: kubevirt
spec:
  selector:
    matchLabels:
      app: dra-test-driver
  updateStrategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: dra-test-driver
    spec:
      priorityClassName: system-node-critical
      serviceAccountName: dra-test-driver
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      containers:
      - name: plugin
        image: registry:5000/kubevirt/test-helpers:devel
        imagePullPolicy: Always
        command: ["/usr/bin/dra-test-driver"]
        securityContext:
          privileged: true
        env:
        - name: NODE_NAME
          valueFrom:
            fieldRef:
              fieldPath: spec.nodeName
        volumeMounts:
        - name: kubelet-plugin
          mountPath: /var/lib/kubelet/plugins/hostpath.dra.kubevirt.io
        - name: kubelet-plugins-registry
          mountPath: /var/lib/kubelet/plugins_registry
        - name: dra-hostpath
          mountPath: /var/run/kubevirt/dra
        - name: cdi
          mountPath: /var/run/cdi
      volumes:
      # The kubeletplugin helper binds dra.sock inside this directory but does
      # not create it, so the per-driver directory must exist beforehand.
      - name: kubelet-plugin
        hostPath:
          path: /var/lib/kubelet/plugins/hostpath.dra.kubevirt.io
          type: DirectoryOrCreate
      - name: kubelet-plugins-registry
        hostPath:
          path: /var/lib/kubelet/plugins_registry
          type: Directory
      - name: dra-hostpath
        hostPath:
          path: /var/run/kubevirt/dra
          type: DirectoryOrCreate
      - name: cdi
        hostPath:
          path: /var/run/cdi
          type: DirectoryOrCreate
