# DKMS configuration for fake-pci
#
# Install with DKMS:
#   sudo cp -r fake-pci /usr/src/fake-pci-1.0
#   sudo dkms add -m fake-pci -v 1.0
#   sudo dkms build -m fake-pci -v 1.0
#   sudo dkms install -m fake-pci -v 1.0
#
# Remove:
#   sudo dkms remove -m fake-pci -v 1.0 --all

PACKAGE_NAME="fake-pci"
PACKAGE_VERSION="1.0"

BUILT_MODULE_NAME[0]="fake-pci"
DEST_MODULE_LOCATION[0]="/extra"

AUTOINSTALL="yes"
